#include <bits/stdc++.h>
// using namespace std;
int count(string data, int a[]){
    int counter=0;
    data = std::tolower(data);
    for(int i=0;i<data.length();i++){

    }
    return counter;
}

int main()
{
    int arr[] = new int[26];
    std::string dog = "There goes the dog!";

    int size = count(dog);
    std::cout << "size: " << size << std::endl;
    return 0;
}