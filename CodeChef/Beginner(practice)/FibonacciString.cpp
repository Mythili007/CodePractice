#include <bits/stdc++.h>
using namespace std;
#define maxOf(a, b) ((a) > (b) ? (a) : (b))
#define minOf(a, b) ((a) < (b) ? (a) : (b))
#define check(a, b, c)                                                         \
  (((a) == (b + c)) || ((b) == (a + c)) || ((c) == (b + a)) ? 0 : 1)

int main(){
    int t;
    
}