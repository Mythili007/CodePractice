#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t-- > 0)
    {
        int n;
        cin >> n;
        int *num = new int[n];
        int *denom = new int[n];
        string *st = new string[n];
        float num, denom;
        for (int i = 0; i < n; i++)
        {
            cin>>num;
            // int a = int(ceil(num));
            cout<<"num: "<<num<<endl;
        }
    }
    return 0;
}