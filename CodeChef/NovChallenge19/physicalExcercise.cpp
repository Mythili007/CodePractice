#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t-- > 0)
    {
        int x, y;
        cin >> x >> y;
        int n, m, k;
        cin >> n >> m >> k;
        int a[2 * n];
        int b[2 * m];
        int c[2 * k];
        for (int i = 0; i < 2 * n; i++)
        {
            cin >> a[i];
        }

        for (int i = 0; i < 2 * m; i++)
        {
            cin >> b[i];
        }

        for (int i = 0; i < 2 * k; i++)
        {
            cin >> c[i];
        }

        
    }
    return 0;
}